# dependencies
node_modules
# npm lockfile at repo root confuses Next/Turbopack root detection;
# the app uses pnpm (portfolio/pnpm-lock.yaml) only.
/package-lock.json

# build artefacts (Altlasten aus der Zeit vor dem Umzug nach portfolio/)
.next/
out/
next-env.d.ts
*.tsbuildinfo

# misc
.DS_Store
*.pem

# env files
.env*

# Screenshot-Ordner des Gerätematrix-Runners (scripts/mobile-matrix.mjs)
portfolio/.baseline/
portfolio/.after/
portfolio/.noise/
portfolio/.shots/
portfolio/.verify-*/
portfolio/.baseline-frozen/
portfolio/.after-frozen/
